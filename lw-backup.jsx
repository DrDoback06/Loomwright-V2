// =====================================================================
// lw-backup.jsx — Project import/export bundle (Phase 11 stub).
// =====================================================================
(function initLwBackup(global) {
  global.Loomwright = global.Loomwright || {};
})(typeof window !== "undefined" ? window : globalThis);
