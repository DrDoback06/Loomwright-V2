name: Inspect final patch validation diff

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/inspect-review-ci.yml
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1
      - name: Capture validation failure tail
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          JOB_ID: "86670133080"
        run: |
          gh api "repos/$REPO/actions/jobs/$JOB_ID/logs" > /tmp/apply.log
          tail -n 70 /tmp/apply.log > .review-apply-failure.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .review-apply-failure.txt
          git commit -m "Record final patch validation diff" || exit 0
          git push origin HEAD:main
