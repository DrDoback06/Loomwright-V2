name: Deploy verified Loomwright extraction release

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/deploy-chatgpt-preview.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

env:
  # Exact source that passed Review Identity CI run 29209640434.
  LOOMWRIGHT_REF: 523c74ca96ff3371dbc11475754c3d539cf51a49
  LW_BASE: /Loomwright-V2/

jobs:
  build:
    name: Package verified context-aware extraction build
    runs-on: ubuntu-latest
    outputs:
      source_sha: ${{ steps.source.outputs.sha }}
    steps:
      - name: Check out the fully verified Loomwright release
        uses: actions/checkout@v4
        with:
          ref: ${{ env.LOOMWRIGHT_REF }}
          fetch-depth: 1

      - name: Record exact source commit
        id: source
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Confirm verified source still compiles
        run: |
          npm run typecheck
          npm run lint
          npm run test:unit
          npm run build

      - name: Prepare GitHub Pages package
        run: |
          cp dist/index.html dist/404.html
          touch dist/.nojekyll
          cat > dist/deployed-version.json <<JSON
          {
            "application": "loomwright-context-aware-extraction",
            "sourceBranch": "agent/review-identity-resolution",
            "sourceSha": "${{ steps.source.outputs.sha }}",
            "verification": {
              "typecheck": "passed",
              "lint": "passed",
              "unitSuite": "passed",
              "browserSuite": "passed-desktop-and-mobile",
              "verifiedRunId": 29209640434
            },
            "extractionRelease": {
              "contextualTypeValidation": true,
              "junkPhraseRejection": true,
              "possessiveCanonicalisation": true,
              "explicitLocationSkillEventCues": true,
              "conservativeSingleTokenDiscovery": true
            }
          }
          JSON

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    name: Publish verified Loomwright extraction release
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
